This docker compose file allows you to build [this forum website](https://github.com/celeschai/forum/tree/main) by pulling images from docker directly.

Ensure that you have docker and docker compose CLI installed

run:

```
docker compose up -d

```

you can change the port numbers and host IP address in .env